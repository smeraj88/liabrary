<h1><%= @subject.title -%>&nbsp;&nbsp; Books:</h1><br>

<table class="table table-hover table-condensed">
   <thead>
      <tr>
         <th>Id</th>
         <th>Name</th>
         
      </tr>
   </thead>
   <tbody>
   
   <% @subject.books.each do |c| %>
   <tr>
      <td><%=c.id%></td>
      <td><%= link_to c.title, :action => "show", :id => c.id -%></td>
     </tr>
   <% end %>
   
   </tbody>
   </table>
<%= link_to 'Back', {:action => 'list'} %>